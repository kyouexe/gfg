import { Link } from 'react-router-dom';
interface NavbarProps {
  onSearch: (query: string) => void;
  searchQuery: string;
}
export const Navbar = ({
  onSearch,
  searchQuery
}: NavbarProps) => {
  return;
};